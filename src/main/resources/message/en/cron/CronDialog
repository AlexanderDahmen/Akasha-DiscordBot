(!) _Section not found_: ${__SECTION__}

[CanYouDoCron]
Can you input a **cron** command? [Yes, No]
( https://en.wikipedia.org/wiki/Cron )

[AskCron]
Input a cron command:

[InvalidCron]
Not a valid cron command: `${cron}`
Would you like to try again? [Yes, No]

[CantCronThis]
Okay, doing manual time input.
This might be a bit cumbersome - you may want to learn cron to make this more efficient.

[ExplainMinuteSecondHour]
(Range `0` through `${max}`, multiple can be seperated by `,`, use `*` for any, use `-` for ranges)

[ExplainDoM]
(Range `1` through `31`, multiple can be seperated by `,`, use `*` for any, use `?` for unknown, use `-` for ranges, use `L` for "Last of Month", use `W` for "Last of Week")

[ExplainMonth]
(Range `1` through `12` or `JAN` through `DEC`, multiple can be seperated by `,`, use `*` for any, use `-` for ranges)

[ExplainDoW]
(Range `1` through `7` or `MON` through `SUN`, multiple can be seperated by `,`, use `*` for any, use `?` for unknown, use `-` for ranges)

[AskSeconds]
$$ ExplainMinuteSecondHour $$
Enter **seconds**:

[AskMinutes]
$$ ExplainMinuteSecondHour $$
Enter **minutes**:

[AskHours]
$$ ExplainMinuteSecondHour $$
Enter **hours**:

[AskDayOfMonth]
$$ ExplainDoM $$
Enter **day of month**:

[AskMonth]
$$ ExplainMonth $$
Enter **month**:

[AskDayOfWeek]
$$ ExplainDoW $$
Enter **day of week**:
